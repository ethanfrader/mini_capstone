json = []
@products.each do |product|
  json << {
    id: product.id,
    name: product.name,
    description: product.description,
    abv: product.abv,
    ounces: product.ounces,
    price: product.price,
  }
end
return json
