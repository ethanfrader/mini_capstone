json = []
if @product != nil
  json << {
    id: @product.id,
    name: @product.name,
    description: @product.description,
    abv: @product.abv,
    ounces: @product.ounces,
    price: @product.price,
  }
else
  json << { message: "No product selected." }
end
return json
