json = []
if @product != nil
  json << {
    name: @product.name,
    description: @product.description,
    abv: @product.abv,
    ounces: @product.ounces,
    price: @product.price,
  }
else
  json << { message: "No product selected." }
end
return json
